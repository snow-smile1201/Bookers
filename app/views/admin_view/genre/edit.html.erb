<div class="container px-sm-0 px-5" >
  <div class="row">
    <h2>ジャンル編集</h2>
    <%= form_with model: @genre, local: true do |f| %>
      <div class="field">
        <label>ジャンル名</label><%= f.text_field :genre_name %><%= f.submit '変更を保存' %>
      </div>
    <% end %>
    

