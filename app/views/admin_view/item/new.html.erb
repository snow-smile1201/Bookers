<h4>New Book</h4>
<%= form_with model: @book do |f| %>
  <label>商品画像</label>
  <label>商品画像</label>
  <label>商品画像</label>
  <label>商品画像</label>
  <label>商品画像</label>
  <label>商品画像</label>
  <div>Title</div>
  <%= f.text_field :title %>

  <div>Body</div>
  <%= f.text_area :body %>
  <%= f.submit 'create book' %>
<% end %>